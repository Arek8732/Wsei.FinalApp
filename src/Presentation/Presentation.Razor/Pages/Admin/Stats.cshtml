@page
@model Presentation.Razor.Pages.Admin.StatsModel
@{
    ViewData["Title"] = "Admin / Stats";
}

<h2>Admin / Stats</h2>

<form method="post" asp-page-handler="CallSoap">
    <button type="submit" class="btn btn-primary">Wywołaj SOAP RegisterUser</button>
</form>

@if (!string.IsNullOrWhiteSpace(Model.SoapResult))
{
<h3>SOAP response</h3>
<pre>@Model.SoapResult</pre>
}
